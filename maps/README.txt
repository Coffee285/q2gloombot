# Navigation node files for q2gloombot
# One .nav file per Gloom map, e.g. gloom1.nav
# These files are generated by the nav-editor tool and are not shipped.
# Without a .nav file, bots roam randomly.
